// ltl-merkle/main.ltl
//
// Merkle tree with verifiable inclusion proofs.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-merkle v0.1.0")
    io::println("Merkle tree with verifiable inclusion proofs.")
    0
}
