// examples/demo.ltl — runnable demo for ltl_merkle

import std::io
use ltl_merkle as lib

fn main() {
    io::println("ltl_merkle demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}